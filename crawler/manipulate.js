var exec = require("child_process").exec;

var Manipulate = function() {
	
};

Manipulate.prototype.handerScript = function(argument) {
	// read the script function and exec it
};

module.exports = Manipulate;